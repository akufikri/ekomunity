on:
  push:
    branches:
      - dev
name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: "ftp.amalprihatinsabah.com"
          username: "ekomunity@ekomuniti.my"
          password: "$&0^pI+w3Uw8t{KW"
          port: 21
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/vendor/**
            .ftpquota